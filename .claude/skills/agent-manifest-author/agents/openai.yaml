interface:
  display_name: "Author Berth Manifests"
  short_description: "Create fleet and pipeline YAML for berth"
  default_prompt: "Use $agent-manifest-author to write or refine a berth fleet or pipeline manifest and validate it with dry runs."
